version: '3'

services:
  es:
    image: elasticsearch:6.8.11
    restart: always
    ports:
      - "9200:9200"
      - "9300:9300"
    environment:
      discovery.type: single-node
    volumes:
      - esdata:/usr/share/elasticsearch/data

volumes:
  esdata:
